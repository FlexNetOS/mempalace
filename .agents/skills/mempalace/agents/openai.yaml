interface:
  display_name: "Mempalace"
  short_description: "Repo-specific patterns and workflows for mempalace"
  default_prompt: "Use the mempalace repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true